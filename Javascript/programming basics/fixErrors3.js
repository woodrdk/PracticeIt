// JavaScript source code
//fixErrors3

// Prints a full name where last is followed by a ", " and the capital letter
// of first, which is followed by a "."
function printName(first, last) {
    String fullname = "";
    fullname = last + ", " + substring(first(0)) + ".";
    return fullname;
}